# dev 环境
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: defaultDataSource
    url: jdbc:mysql://localhost:3306/campus-graduation-design?serverTimezone=UTC
    username: root
    password: admin1234

server:
  port: 8080